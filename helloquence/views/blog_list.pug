extends layout

block content
  h1= title

  ul
    each blog in blog_list
      li
        a(href=blog.url) #{blog.title}
        |  (#{blog.author.first_name} #{blog.author.last_name})

    else
      li There are no blogs.
